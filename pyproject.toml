[project]
name = "monocle"
version = "0.1.0"
dependencies = [
    "jupyterlab",
    "jupytext",
    "pandas",
    "requests",
    "xlsx2csv",
    "yfinance",
]

[tool.setuptools]
packages = ["utils"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
