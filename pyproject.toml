[project]
name = "monocle"
version = "0.1.0"
dependencies = [
    "jupyterlab",
    "requests",
]

[tool.setuptools]
packages = ["utils"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
